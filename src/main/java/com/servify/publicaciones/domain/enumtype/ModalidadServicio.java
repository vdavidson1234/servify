package com.servify.publicaciones.domain.enumtype;


/// enum que representa las modalidades de servicio que puede ofrecer un profesional,
///  como presencial, virtual o mixta
public enum ModalidadServicio {
    PRESENCIAL,
    VIRTUAL,
    MIXTA
}